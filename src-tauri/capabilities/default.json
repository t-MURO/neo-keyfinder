{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window can call explicitly registered Tauri commands only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
