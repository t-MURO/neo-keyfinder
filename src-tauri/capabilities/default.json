{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Batch windows can call explicitly registered Tauri commands only.",
  "windows": ["main", "batch-*"],
  "permissions": ["core:default"]
}
